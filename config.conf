[proxy]
host = 0.0.0.0
port = 8000

[apache]
host = 127.0.0.1
port = 80

[cache]
expiration_time = 60

[dashboard]
host = 127.0.0.1
port = 8080
# Uncomment the line below to enable token authentication for the dashboard.
# When set, every API request must include the header:  Authorization: Bearer <token>
# token = mysecrettoken
